# Core — required by run.py
numpy>=1.26
polars>=0.20
scikit-learn>=1.4
xgboost>=2.0
lightgbm>=4.0
pyarrow>=14.0

# Notebooks — required only by notebooks/*.ipynb
# Installed automatically with `pip install -r requirements.txt`.
# run.py does not need these.
jupyterlab>=4.0
matplotlib>=3.7
pandas>=2.0
